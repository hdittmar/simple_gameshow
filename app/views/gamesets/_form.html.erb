<%= form_with(model: gameset, local: true) do |form| %>
  <% if gameset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gameset.errors.count, "error") %> prohibited this gameset from being saved:</h2>

      <ul>
        <% gameset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
